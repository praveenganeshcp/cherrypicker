import { requestManagerCore } from './request-manager-core';

describe('requestManagerCore', () => {
  it('should work', () => {
    expect(requestManagerCore()).toEqual('request-manager-core');
  });
});
